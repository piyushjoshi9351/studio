version: '3.8'

services:
  app:
    build: .
    ports:
      - "9002:9002"
    environment:
      - PORT=9002
      - NEXT_TELEMETRY_DISABLED=1
    volumes:
      - ./:/app
      - /app/node_modules
    command: npm run dev
